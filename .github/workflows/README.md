# K8s APP

This is a very simple application for demonstrating Kubernetes and other related tools.

## How to Use

1. Run the file **create_repo.sh** in the terminal.

run like so:

sudo chmod u+x create_repo.sh
sudo ./create_repo.sh
2. Push the code to the repository.


## Author: David
